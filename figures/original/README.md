# Canonical original project figures

This directory contains repository-preview copies derived directly from the original project figure exports. These are the canonical visual assets for the public repository. They must not be replaced by simplified/generated substitute plots when the original project export exists.

The source project retains the publication-quality PNG/PDF versions; JPG files here are lightweight GitHub previews of those same original figures.
